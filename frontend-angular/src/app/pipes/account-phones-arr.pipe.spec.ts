import { AccountPhonesArrPipe } from './account-phones-arr.pipe';

describe('AccountPhonesArrPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountPhonesArrPipe();
    expect(pipe).toBeTruthy();
  });
});
