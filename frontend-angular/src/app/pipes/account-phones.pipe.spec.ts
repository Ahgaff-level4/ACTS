import { AccountPhonesPipe } from './account-phones.pipe';

describe('AccountPhonesPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountPhonesPipe();
    expect(pipe).toBeTruthy();
  });
});
