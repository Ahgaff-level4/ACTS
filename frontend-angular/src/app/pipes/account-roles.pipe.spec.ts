import { AccountRolesPipe } from './account-roles.pipe';

describe('AccountRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
