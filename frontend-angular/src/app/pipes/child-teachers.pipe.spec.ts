import { ChildTeachersPipe } from './child-teachers.pipe';

describe('ChildTeachersPipe', () => {
  it('create an instance', () => {
    const pipe = new ChildTeachersPipe();
    expect(pipe).toBeTruthy();
  });
});
