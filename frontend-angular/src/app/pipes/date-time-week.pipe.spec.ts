import { DateTimeWeekPipe } from './date-time-week.pipe';

describe('DateTimeWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
